{
    "name": "SynergiTech/twice",
    "description": "A fork of spatie/once, just renamed to twice to avoid conflicting with the once() helper function in Laravel 11 which isn't as full features as spatie/once",
    "keywords": [
        "spatie",
        "once",
        "callable",
        "memoization",
        "cache"
    ],
    "homepage": "https://github.com/SynergiTech/twice",
    "license": "MIT",
    "authors": [
        {
            "name": "Freek Van der Herten",
            "email": "freek@spatie.be",
            "homepage": "https://spatie.be",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.0"
    },
    "require-dev": {
        "pestphp/pest": "^1.21",
        "symfony/var-dumper": "^5.1"
    },
    "autoload": {
        "psr-4": {
            "Spatie\\Once\\": "src"
        },
        "files": [
            "src/functions.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Spatie\\Once\\Test\\": "tests"
        }
    },
    "scripts": {
        "test": "vendor/bin/pest"
    },
    "config": {
        "sort-packages": true
  }
}
